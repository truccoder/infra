# Địa chỉ site lấy từ biến môi trường PUBLIC_DOMAIN (đặt trong docker/.env).
#
#   chưa có domain:  để trống  ->  mặc định `:80`, chạy HTTP thuần đúng như hiện tại
#   có domain:       PUBLIC_DOMAIN=socialapp.example.com
#                    -> Caddy tự xin chứng chỉ Let's Encrypt, bật HTTPS, tự gia hạn, và tự
#                       chuyển hướng HTTP sang HTTPS
#
# Trước đây file này có sẵn một khối domain nằm dạng comment để "bỏ comment khi có domain". Cách
# đó nghĩa là hai bản định tuyến song song tồn tại, và bản đang bị comment sẽ lặng lẽ lạc hậu mỗi
# lần ai đó sửa bản đang chạy. Giờ chỉ còn một khối, khác nhau đúng ở địa chỉ site.
#
# ⚠️ Chuyển sang domain KHÔNG chỉ là đổi biến này. Phải làm cùng lúc:
#   1. Trỏ bản ghi DNS A của domain về IP máy chủ và đợi phân giải được — Caddy xin chứng chỉ
#      bằng thử thách HTTP, sẽ thất bại nếu DNS chưa trỏ về đây.
#   2. Mở cổng 443 trên tường lửa. Cổng 80 VẪN phải mở, ACME dùng nó để xác thực.
#   3. Đổi PUBLIC_BASE_URL trong .env sang https://<domain>. Quên bước này thì link đặt lại mật
#      khẩu và link xác thực email gửi cho người dùng vẫn trỏ về IP cũ qua HTTP.
#   4. Đăng ký lại redirect URI trong console Google và GitHub, khớp từng ký tự.
# Thứ tự đầy đủ nằm ở docs/domain-cutover.md.
#
# ⚠️ DÒNG ĐỊA CHỈ SITE DƯỚI ĐÂY KHÔNG BAO GIỜ ĐƯỢC NỞ RA THÀNH CHUỖI RỖNG.
#
# `{$PUBLIC_DOMAIN::80}` trông như "rỗng thì dùng :80", nhưng giá trị mặc định của Caddy CHỈ áp
# dụng khi biến KHÔNG TỒN TẠI. Biến tồn tại mà rỗng thì Caddy thay vào chuỗi rỗng, dòng này rút
# lại còn mỗi ` {`, và một Caddyfile mở đầu bằng `{` được hiểu là khối global options — chỉ thị
# `handle` ngay dưới lập tức sai ngữ cảnh và Caddy chết ngay lúc nạp config:
#
#   Error: adapting config using caddyfile: /etc/caddy/Caddyfile:21: unrecognized global option: handle
#
# Không ai nghe cổng 80, trình duyệt báo ERR_CONNECTION_REFUSED. Đó là sự cố ngày 2026-08-22.
# Việc bảo đảm biến luôn có giá trị hợp lệ nằm ở docker-compose.prod.yml (`${PUBLIC_DOMAIN:-:80}`,
# vì `:-` của Compose coi rỗng như chưa đặt) — đừng đổi nó về `${PUBLIC_DOMAIN:-}`.
{$PUBLIC_DOMAIN::80} {
	handle /v1/* {
		reverse_proxy backend:8080
	}

	handle /peerjs/* {
		reverse_proxy web-ui:3000
	}

	handle {
		reverse_proxy web-ui:3000
	}
}

# ─── MinIO sau proxy (chỉ làm được sau khi đã có domain) ─────────────────────────────────────
#
# Hiện MinIO publish thẳng cổng 9000 ra Internet. Muốn đóng cổng đó thì bỏ comment khối dưới —
# nhưng bắt buộc dùng MỘT HOSTNAME RIÊNG, không được dùng đường dẫn con kiểu /files/*.
#
# Lý do: link tải sách là presigned URL, chữ ký của nó tính trên cả host lẫn đường dẫn đầy đủ.
# Thêm một tiền tố đường dẫn là hỏng toàn bộ chữ ký. Hostname riêng giữ nguyên đường dẫn nên
# chữ ký vẫn hợp lệ.
#
# Kèm theo, phải viết lại các URL ảnh đại diện đã lưu trong database — chúng giữ địa chỉ tuyệt đối
# tại thời điểm upload và không tự đổi theo. Xem scripts/rewrite-minio-urls.sql.
#
# Bỏ comment thì phải đặt MINIO_PUBLIC_DOMAIN thành một hostname THẬT. Nó dính đúng cái bẫy vừa
# nói ở trên và còn tệ hơn: ở đây không có giá trị mặc định nào đỡ cho, nên một biến rỗng biến
# dòng dưới thành ` {` và giết cả tiến trình Caddy — kể cả site chính vốn đang chạy tốt.
#
# {$MINIO_PUBLIC_DOMAIN} {
# 	reverse_proxy minio:9000
# }
