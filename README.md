## Architecture

```
                    Internet
                       │
                   ┌───▼───┐
                   │ Caddy  │  :80 / :443
                   │ Proxy  │
                   └───┬───┘
              ┌────────┴────────┐
         /v1/*│                 │ /*
        ┌─────▼─────┐   ┌──────▼──────┐
        │  Backend   │   │   Web UI    │
        │ :8080      │   │   :3000     │
        └─────┬──────┘   └─────────────┘
              │
    ┌─────────┼──────────┬──────────┬──────────┐
    │         │          │          │          │
┌───▼──┐ ┌───▼──┐ ┌─────▼─┐ ┌─────▼────┐ ┌───▼──┐
│Postgr│ │Redis │ │ Neo4j │ │OpenSearch│ │MinIO │
└──────┘ └──────┘ └───────┘ └──────────┘ └──────┘
```

---

## CI/CD Flow

```
# Code changes (backend hoặc web-ui repo)
Push to main → Build Docker image (ARM64) → Push to GHCR → SSH deploy

# Config changes (infra repo)
Push to main → Upload docker-compose + Caddyfile → Restart services
```